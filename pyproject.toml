[project]
name = "hello-automation"
version = "0.1.0"
description = "Seqera Platform action-chaining demo: create-samplesheet triggers Sarek through the action launch endpoint"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "seqerakit>=0.5.2",
]
