# TODO
credentials:
    - type: "aws"
      name: "scidev-playground"
      workspace: "SLAS2025/Demo"
      access-key: "$AWS_ACCESS_KEY_ID"
      secret-key: "$AWS_SECRET_ACCESS_KEY"
      assume-role-arn: "$AWS_ASSUME_ROLE_ARN"
      overwrite: True
