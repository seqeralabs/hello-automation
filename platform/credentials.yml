# TODO
credentials:
    - type: "aws"
      name: "scidev-playground"
      workspace: "hello-automation/Demo"
      access-key: "$AWS_ACCESS_KEY_ID"
      secret-key: "$AWS_SECRET_ACCESS_KEY"
      # NOTE Not needed assume-role-arn: "$AWS_ASSUME_ROLE_ARN"
      overwrite: False
